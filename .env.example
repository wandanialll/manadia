# =============================================================
# ManaDia Environment Configuration
# =============================================================
# Copy this file to .env and fill in the values.
# NEVER commit .env to git — it's in .gitignore.
# =============================================================

# ----- Database Configuration (REQUIRED) -----
# Generate a strong password on the server:
#   openssl rand -base64 24
DB_PASSWORD=CHANGE_ME

# Optional database configuration (defaults provided)
# DB_USER=manadia
# DB_HOST=db
# DB_PORT=5432
# DB_NAME=manadia

# ----- Caddy BasicAuth (REQUIRED) -----
# Generate bcrypt hashes on the server using the Caddy container:
#   docker run --rm caddy:latest caddy hash-password --plaintext 'YOUR_PASSWORD'
#
# Paste the full $2a$... or $2b$... hash below.
CADDY_OWNTRACKS_PASSWORD_HASH=CHANGE_ME
CADDY_ADMIN_PASSWORD_HASH=CHANGE_ME

# ----- API Key Configuration -----
# Set to 0 for keys that never expire, or number of days until expiration
API_KEY_EXPIRATION_DAYS=365

# ----- Logging -----
LOG_LEVEL=INFO

# ----- Image Tag (optional) -----
# MANADIA_IMAGE_TAG=latest
