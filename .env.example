# Database Configuration (REQUIRED)
# Generate a strong password: openssl rand -base64 32
DB_PASSWORD=your_secure_postgres_password_here

# Optional database configuration (defaults provided)
DB_USER=manadia
DB_HOST=db
DB_PORT=5432
DB_NAME=manadia

# Caddy Authentication (REQUIRED)
# Generate password hashes:
#   For /pub endpoint (OwnTracks):
#     htpasswd -bc - owntracks yourpassword | cut -d: -f2 | base64
#   For /admin endpoints:
#     htpasswd -bc - admin yourpassword | cut -d: -f2 | base64
#
# Or use: echo -n "username:password" | md5sum
CADDY_OWNTRACKS_PASSWORD_HASH=your_owntracks_password_hash_here
CADDY_ADMIN_PASSWORD_HASH=your_admin_password_hash_here

# API Key Configuration
# Set to 0 for keys that never expire, or number of days until expiration
API_KEY_EXPIRATION_DAYS=365

# Logging (optional)
LOG_LEVEL=INFO

# Container Image Digest (optional, for reproducible builds)
# Pin to specific digest instead of 'latest' tag
# MANADIA_IMAGE_DIGEST=sha256:abc...

# Docker Image Registry (optional)
# For private registries, Docker login required beforehand
# REGISTRY=ghcr.io
